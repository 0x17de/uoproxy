package packets

/*

package packets

type Packet struct {
	BasePacket
}

func (p *Packet) Read(in chan byte) {
}

func (p *Packet) Bytes() []byte {
	return p.Writer().
		bytes()
}

*/
